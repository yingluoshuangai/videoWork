<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xyz.xnmq.inf.users.mapper.UserSearchRecordMapper">

    <select id="findHotContent" resultType="string">
        select content
        from user_search_record
        where disabled = 0 group by content
        order by count(content) desc limit #{number};
    </select>

</mapper>